{% macro includeView(webIncludes, location, templateVars = {}) %}
    {% for file in webIncludes %}
        {% if (file.position == location) %}
            {% include['WebCreator/Include/' ~ file.path, ''] with templateVars %}
        {% endif %}
    {% endfor %}
{% endmacro %}